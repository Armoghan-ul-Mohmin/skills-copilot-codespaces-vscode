function calculateNumbers(a, b) {
    var c = a + b;
    return c;
}

// Predefined values
var a = 5;
var b = 10;

// Call the function with predefined values
var result = calculateNumbers(a, b);

// Print a pretty output
console.log(`The sum of ${a} and ${b} is ${result}.`);